---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<blockquote
  class:list={[
    'border border-white/8 border-l-[3px] border-l-primary bg-white/[0.03] px-6 py-5 text-base leading-relaxed text-foreground',
    className,
  ]}
>
  <slot />
</blockquote>
