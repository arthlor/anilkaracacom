---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span class="inline-flex min-h-[26px] items-center rounded-full bg-white/[0.04] px-2.5 py-0.5 text-[0.68rem] font-medium tracking-[0.02em] text-primary/80">
  {label}
</span>
